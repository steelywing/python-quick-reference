---
layout: none
---
[
    {% for page in site.collections.docs.docs %}
        {
            "title": {{ page.title | jsonify }},
            "url": {{ page.url | jsonify }},
            "weight": {{ page.weight | jsonify }},
            "content": {{ page.output | jsonify }}
        }{% unless forloop.last %},{% endunless %}
    {% endfor %}
]
